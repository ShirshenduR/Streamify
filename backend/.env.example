DATABASE_URL=postgresql://user:password@localhost:5432/your_local_db # Replace with your actual database URL if using PostgreSQL

# Django settings
SECRET_KEY=your-django-secret-key
DEBUG=True

# YTMusic API authentication
# Paste your full YTMusic cookie string here (see instructions below)
YTMUSIC_COOKIE=

# ---
# How to get your YTMusic cookie:
# 1. Go to https://music.youtube.com in Chrome and log in.
# 2. Open DevTools (F12) → Application tab → Cookies → https://music.youtube.com
# 3. Copy the value of the following cookies: SAPISID, HSID, SSID, SID, APISID, etc.
# 4. Paste the full cookie string (or the contents of headers_auth.json) above.
#
# Never commit your real .env with secrets to version control!